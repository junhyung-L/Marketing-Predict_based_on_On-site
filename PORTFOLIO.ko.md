# 구매 전환 예측: 마케팅 이론을 고객별 동태 변수로 바꾼 CRM 분석

[English](PORTFOLIO.md) | [한국어](PORTFOLIO.ko.md)

## 한눈에 보기

전환율이 0.1234%에 불과한 이커머스 CRM 환경에서는 “누가 구매할까”라는 질문만으로는 실무적인 타겟팅이 어렵다. 이 프로젝트는 대규모 메시지 로그에 담긴 고객의 구매 주기, 반응 시점, 누적 노출 피로, 채널·콘텐츠 경험을 수치화해 구매 전환 예측에 넣었다. 핵심은 더 복잡한 신경망을 쌓는 대신, 마케팅 이론을 모델이 읽을 수 있는 변수로 바꿔 본 점이다.

논문 원고는 도메인 변수를 포함한 XGBoost가 원분포 홀드아웃에서 F1 0.3806, PR-AUC 0.3620을 기록했다고 보고한다. 베이스라인 대비 재현율은 30.74%에서 52.30%로 높아졌다. 이 수치는 특정 공개 CRM 로그와 해당 홀드아웃 설계에서의 분류 결과이며, 실제 매출이나 발송비 절감이 확정됐다는 뜻은 아니다.

## 문제를 다시 정의한 방식

일반적인 고객 분류는 최근 구매일이나 누적 구매액처럼 정적인 요약값에 기대기 쉽다. 하지만 메시지를 받은 고객은 같은 사람이라도 구매 직후인지, 평소 활동 시간대인지, 같은 주제의 메시지를 연달아 받았는지에 따라 반응이 달라진다. 프로젝트는 이 차이를 네 개의 동태 변수군으로 정리했다.

| 변수군 | 구현한 신호 | 모델에 넣은 이유 |
|---|---|---|
| 구매·재구매 주기 | 개인별 구매 간격, 재구매 준비도, 구매 직후 불응기 | 고객이 자기 구매 사이클의 어느 지점에 있는지 반영 |
| 시계열 반응 | 선호 시간대·요일 일치도, 급여일·분기말 근접도 | 발송 시점과 고객의 반응 맥락을 구분 |
| 마케팅 피로도 | 채널별 반감기를 적용한 누적 노출, 마지막 수신 후 경과 시간 | 반복 노출과 회복 시간을 함께 반영 |
| 채널 효과성 | 콘텐츠 주제 신선도, 과거 구매 직전 채널 경로와의 유사도 | 무작정 발송하기보다 적절한 접점을 가려내기 위함 |

피로도는 단순한 “수신 횟수”가 아니다. 최근 메시지에 더 큰 가중치를 주고 시간이 지나면 영향이 줄어드는 지수 감쇠 합으로 계산했다. 채널마다 침해성과 회복 속도가 다르다는 가정도 반영했다. 따라서 이 변수는 인과적으로 “메시지가 피로를 만들었다”는 판정이 아니라, 과거 노출 이력에서 다음 전환을 구분하는 예측 신호다.

## 데이터와 전처리 범위

논문 원고의 분석 대상은 Kaggle REES46의 *E-commerce multichannel direct messaging 2021–2023*이다. 원고 기준으로 2021~2023년 사이 약 2년 동안 수집된 메시지·반응 로그 1억 7,200만 건, 고객 약 185만 명, 마케팅 캠페인 1,907개가 포함된다. 메시지 전송·반응 로그, 캠페인 메타데이터, 고객 속성, 외부 시점 정보의 네 관계형 테이블을 결합했다.

전처리에서는 고객 프로파일·프로모션과 직접 연결된 마케팅 캠페인만 남기고, 단순 거래 알림과 같은 운영성 메시지는 분석 대상에서 제외했다. 이어 고객별 과거 상호작용을 예측 시점 직전까지의 순서로 재구성해 파생변수를 계산했다. 논문 설계는 원분포를 유지한 채 층화 70%/15%/15% 학습·검증·테스트 분할을 사용하고, 대치·스케일링 통계량을 학습 세트에서만 산출했다고 명시한다.

현재 저장소에 함께 보관한 실행 경로는 더 작은 `final_data_100k_64.parquet`을 대상으로 한다. `RUN_MANIFEST.md`에는 시드 1, 층화 15% 홀드아웃, 3-fold `StratifiedKFold`가 기록되어 있고, 보관된 `result_xgb.json`은 홀드아웃 1,499,900행의 혼동행렬과 64개 변수 결과를 담는다. 이것은 원고의 대규모 실험과 같은 데이터 규모·분할이 아니므로, 두 결과를 같은 재현 실험으로 합산하지 않았다.

## 비교 실험과 검증 기준

트리 모델(Random Forest, XGBoost), MLP, CNN, RNN, LSTM, CNN-LSTM, RNN-LSTM, TabNet까지 총 9개 모델을 비교했다. 희소 양성 클래스를 정확도 하나로 평가하면 대부분을 음성으로 예측해도 좋아 보일 수 있어, PR-AUC·F1·F2·재현율을 함께 봤다. 원고는 PR-AUC를 목표로 Optuna 기반 탐색과 5-fold 교차검증, 조기 종료·프루닝을 사용했다고 기술한다.

| XGBoost 원분포 홀드아웃 결과 | 베이스라인 변수 | 도메인 변수 포함 |
|---|---:|---:|
| Precision | 0.4199 | 0.2991 |
| Recall | 0.3074 | 0.5230 |
| F1 | 0.3550 | 0.3806 |
| F2 | 0.3248 | 0.4549 |
| PR-AUC | 0.3177 | 0.3620 |

정밀도는 낮아졌지만 재현율과 F계열 지표가 개선된 것은, 극도로 희소한 전환을 더 많이 포착하도록 판단 경계가 바뀌었기 때문이다. 어느 쪽이 더 낫다고 단정하려면 실제 캠페인의 접촉 비용과 오발송 비용이 추가로 필요하다. 논문에서 보이는 13.1%는 TabNet의 **F2-score**가 0.3659에서 0.4138로 바뀐 비율이며, 마케팅 비용 절감률이 아니다.

## 타겟팅 결과를 읽는 법

원고의 상위 K 타겟팅 분석에서는 예측 점수 상위 0.78%만 선택했을 때 전체 전환의 95.83%를 포착하고, 전체 후보 대비 발송 대상 수가 99.22% 줄어든다고 보고한다. 계산의 분모는 전체 메시지 후보, 분자는 상위 점수로 제한한 후보 수다. 따라서 이는 **동일한 홀드아웃에서의 모의 선별 결과**다. 실제 캠페인에서 메시지를 덜 보냈다는 기록도, 단가를 곱한 금액 절감도, 증분 전환을 확인한 대조군도 저장소에는 없다.

SHAP 분석과 변수 절삭 실험은 왜 이런 예측이 나왔는지 살펴보는 데 사용했다. 피로도·시계열 반응은 비구매 후보를 먼저 거르는 신호로, 재구매 주기와 최근 고관여 행동은 최종 전환 가능성을 높이는 신호로 해석됐다. 이 해석도 관찰 로그에서 나온 연관성이다. 고객에게 메시지를 보냈을 때 생기는 순증 효과를 주장하려면 무작위 실험이나 업리프트 설계가 필요하다.

## 남긴 것과 다음 단계

저장소에는 전처리, XGBoost, 신경망 베이스라인, 반감기 민감도 분석 코드와 JSON/CSV 형태의 요약 결과가 남아 있다. 원본 Parquet와 학습된 모델 파일은 포함되지 않아 누구나 즉시 대규모 실험을 재실행할 수는 없다. 다음 실험에서는 데이터 버전, 실제 사용 행 수, 캠페인 단가, 분할 시점과 패키지 버전을 하나의 매니페스트에 고정하고, 무작위 홀드아웃 대신 시간 순서 검증과 대조군을 추가하는 것이 우선이다.

## 근거 파일

- 재현 경계: [`research/RUN_MANIFEST.md`](research/RUN_MANIFEST.md)
- 저장된 XGBoost 결과: [`research/predict/result_xgb.json`](research/predict/result_xgb.json)
- 실행 코드: [`research/preprocessing_fast.py`](research/preprocessing_fast.py), [`research/xgb.py`](research/xgb.py), [`research/run_xgb_tau_sweep.py`](research/run_xgb_tau_sweep.py)
